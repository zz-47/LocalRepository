
// definition of encapsulation :
 //                             Encapsulation is wrapping up of data & member functions in a single unit called class.


  //        AS the name suggests encapsulation is creation of a cupsule of relevant data ,but of different datatype

      //         .i.e we take data of different datatypes(in,float,char etc) and member functions in a capsule

    // encapsulation is just like using a Class with its full potential

// we can use encapsulation for hidding data as if we use encapsulation in a banking system by using private access modifiier

#include <iostream>
#include <string>
    
     using namespace std;

     // creating a class for a bank user Account.

     class account
     {
       private:
       
        int balance;
         string password;  
            
            // we used data hiding.
         
         public:

            string accountId;
             string userName;
     }


    int main()
    {
      /* ------------------------------------------------------------------------
         --------------------------   code --------------------------------------
         ------------------------------------------------------------------------
       */

      // code represents the printing functions and object creations.
       return 0;
    }