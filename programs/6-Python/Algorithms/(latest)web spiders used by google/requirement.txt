Sure! Below is a simple example of a web spider in Python. A web spider, also known as a web crawler or scraper, is a program that automatically visits web pages, extracts information, and can navigate through the links on those pages to crawl other pages.

We'll use the requests library to fetch the web pages and BeautifulSoup from bs4 to parse the HTML content. Here's how you can create a simple web spider:

Prerequisites
Install the required libraries:

bash
Copy
Edit
pip install requests beautifulsoup4